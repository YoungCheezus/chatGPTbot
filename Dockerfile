# Устанавливаем базовый образ Node.js
FROM node

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем все файлы проекта в рабочую директорию
COPY . .

# Устанавливаем зависимости
RUN npm i

# Установка переменной окружения для определения номера порта, который будет использоваться вашим приложением
# ENV PORT=3000

# Объявление порта
EXPOSE 3000

# Запускаем приложение
CMD [ "npm", "start" ]